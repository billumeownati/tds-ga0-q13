name: My GitHub Action

on:
  workflow_dispatch:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 24f1002231@ds.study.iitm.ac.in
        run: echo "Hello, world!"
